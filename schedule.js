const DAYS=["Lunes","Martes","Miércoles","Jueves","Viernes","Sábado","Domingo"];
function classesDay(d){return LifeOS.state.classes.filter(x=>+x.day===d).sort((a,b)=>a.start.localeCompare(b.start))}
function reminderText(n){return n<60?n+" min":n===60?"1 hora":n===90?"1 h 30":Math.floor(n/60)+" h"}
function classForm(c){return `<div class="form"><label>Asignatura<input name="title" required value="${LifeOS.escapeHtml(c?.title||"")}" placeholder="Ej. Programación Computacional"></label><div class="two"><label>Día<select name="day">${DAYS.map((d,i)=>`<option value="${i}" ${+c?.day===i?"selected":""}>${d}</option>`).join("")}</select></label><label>Sala<input name="room" value="${LifeOS.escapeHtml(c?.room||"")}" placeholder="Ej. 204"></label></div><div class="two"><label>Inicio<input type="time" name="start" value="${c?.start||"08:30"}" required></label><label>Término<input type="time" name="end" value="${c?.end||"10:40"}" required></label></div><label>Profesor<input name="teacher" value="${LifeOS.escapeHtml(c?.teacher||"")}" placeholder="Opcional"></label><label>Recordatorio<select name="reminder">${[15,30,60,90,120].map(n=>`<option value="${n}" ${+(c?.reminder??LifeOS.state.profile.reminder)===n?"selected":""}>${reminderText(n)} antes</option>`).join("")}</select></label><div class="form-actions"><button type="button" class="soft" data-close>Cancelar</button>${c?'<button type="button" class="danger" id="deleteClass">Eliminar</button>':""}<button class="primary">Guardar</button></div></div>`}
function taskForm(t){return `<div class="form"><label>Objetivo<input name="title" required value="${LifeOS.escapeHtml(t?.title||"")}" placeholder="Ej. Estudiar C"></label><div class="two"><label>XP<input name="xp" type="number" min="1" value="${t?.xp||50}"></label><label>Fecha<input name="date" type="date" value="${t?.date||LifeOS.today()}"></label></div><div class="form-actions"><button type="button" class="soft" data-close>Cancelar</button><button class="primary">Guardar</button></div></div>`}